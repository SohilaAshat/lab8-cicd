version: '3'

services:
  app:
    build: .          
    ports:
      - "3000:3000"
    environment:
      - DB_HOST=db
    depends_on:
      - db

  db:
    image: mongo
    volumes:
      - db_data:/data/db

volumes:
  db_data:
